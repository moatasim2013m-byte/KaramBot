/* shifts-ai.store — tracking IDs, the ONE place they live. Loaded by every page (built pages via the template,
 * /privacy and /data-deletion directly) before analytics.js. All IDs are public by design.
 * Meta Pixel "SHIFT Website" · GA4 property 553710715 · no Google Ads account yet (leave googleAdsId empty —
 * no AW- tag until SHIFT has its own account). Domain verification is a literal tag in the template, not here. */
window.SHIFT_CONFIG = {
  metaPixelId: "1456547816531153",
  ga4Id: "G-Z2F36JFJCN",
  googleAdsId: "",
  googleAdsLabels: { lead: "" },
  metaDomainVerification: ""
};
