node_modules/
dist/
*.tar.gz
.DS_Store

# Environment and credential files
.env
.env.*
*.env
credentials.json
*.pem
*.key
.credentials

# Allow the committed example env file (placeholders only — never real secrets)
!backend/.env.example
